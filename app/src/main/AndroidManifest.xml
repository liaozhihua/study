<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.study">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/base_network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Study">
        <activity
            android:name=".design_pattern.mvp.MvpActivity"
            android:exported="false" />
        <activity
            android:name=".design_pattern.mvvm.UserListActivity"
            android:exported="false" />
        <activity
            android:name=".design_pattern.mvc.ContorllerActivity"
            android:exported="false" />
        <activity
            android:name=".compose.ComposeActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity2"
            android:enabled="true">
            <intent-filter>
                <action android:name="dddddddddddddddddddd" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>